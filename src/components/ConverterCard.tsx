import type { LucideIcon } from 'lucide-react'
export type ConverterItem = { id:string; name:string; description:string; icon:LucideIcon; accent:string }
export function ConverterCard({ item, active, onClick }:{item:ConverterItem;active:boolean;onClick:()=>void}) { const Icon=item.icon; return <button onClick={onClick} className={`group min-w-[205px] rounded-2xl border p-4 text-left transition-all ${active?'border-brand-500 bg-brand-50 shadow-sm ring-2 ring-brand-500/10 dark:bg-brand-500/10':'border-slate-200 bg-white hover:-translate-y-0.5 hover:border-brand-300 hover:shadow-md dark:border-slate-800 dark:bg-slate-900'}`}><span className={`mb-3 flex h-9 w-9 items-center justify-center rounded-xl ${item.accent}`}><Icon size={19}/></span><span className="block text-sm font-bold text-slate-900 dark:text-white">{item.name}</span><span className="mt-1 block text-xs leading-5 text-slate-500 dark:text-slate-400">{item.description}</span></button> }
