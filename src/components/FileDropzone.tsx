import { FileUp, UploadCloud } from 'lucide-react'; import { useRef, useState } from 'react'
export function FileDropzone({ onFile, accept, file }:{onFile:(file:File)=>void;accept?:string;file?:File|null}) { const ref=useRef<HTMLInputElement>(null); const [drag,setDrag]=useState(false); const choose=(files:FileList|null)=>{if(files?.[0])onFile(files[0])}; return <div onClick={()=>ref.current?.click()} onDragOver={e=>{e.preventDefault();setDrag(true)}} onDragLeave={()=>setDrag(false)} onDrop={e=>{e.preventDefault();setDrag(false);choose(e.dataTransfer.files)}} className={`cursor-pointer rounded-2xl border-2 border-dashed px-5 py-8 text-center transition ${drag?'border-brand-500 bg-brand-50 dark:bg-brand-500/10':'border-slate-300 hover:border-brand-400 dark:border-slate-700'}`}><input ref={ref} hidden type="file" accept={accept} onChange={e=>choose(e.target.files)}/>{file?<><FileUp className="mx-auto mb-2 text-brand-500"/><p className="font-semibold text-slate-800 dark:text-white">{file.name}</p><p className="text-xs text-slate-500">{(file.size/1024).toFixed(1)} KB · Click to replace</p></>:<><UploadCloud className="mx-auto mb-2 text-brand-500"/><p className="font-semibold text-slate-800 dark:text-white">Drop a file here or <span className="text-brand-600">browse</span></p><p className="mt-1 text-xs text-slate-500">Your file stays on this device</p></>}</div> }
