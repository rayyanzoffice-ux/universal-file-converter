import { AlertCircle, FileOutput } from 'lucide-react'; import type { ReactNode } from 'react'; import { CopyButton, DownloadButton } from './Buttons'
export function TextAreaPanel({value,onChange,label,placeholder}:{value:string;onChange:(v:string)=>void;label:string;placeholder?:string}) { return <section className="panel"><div className="panel-head"><h3>{label}</h3><span>{value.length.toLocaleString()} chars</span></div><textarea value={value} onChange={e=>onChange(e.target.value)} placeholder={placeholder} spellCheck={false}/></section> }
export function OutputPanel({value,mime,extension,label='Output'}:{value:string;mime:string;extension:string;label?:string}) { return <section className="panel"><div className="panel-head"><h3>{label}</h3><div className="flex gap-2"><CopyButton value={value}/><DownloadButton value={value} mime={mime} extension={extension}/></div></div>{value?<pre>{value}</pre>:<div className="empty"><FileOutput/><p>Your converted output will appear here</p></div>}</section> }
export function ErrorMessage({message}:{message:string}) { return <div role="alert" className="flex items-start gap-2 rounded-xl border border-red-200 bg-red-50 p-3 text-sm text-red-700 dark:border-red-900 dark:bg-red-950/40 dark:text-red-300"><AlertCircle className="mt-0.5 shrink-0" size={17}/>{message}</div> }
export function AppLayout({header,children}:{header:ReactNode;children:ReactNode}) { return <div className="min-h-screen bg-slate-50 text-slate-700 transition-colors dark:bg-slate-950 dark:text-slate-300">{header}<main>{children}</main></div> }
